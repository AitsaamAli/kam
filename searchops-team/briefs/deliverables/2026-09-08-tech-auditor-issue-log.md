# Technical Auditor — Issue Log

**Role:** tech-auditor | **Date:** 2026-09-08 | **Workflow:** onboard (5/8)
**Method:** live checks run against the actual project files this session (not from memory alone) — file counts, `blogPosts` array parsing, sitemap tag counts, robots.txt content.

| Issue | Severity | Affected | Why it matters | Fix | Effort |
|---|---|---|---|---|---|
| **719 stale "Redirecting..." placeholder entries in `blogs.html`'s `blogPosts` array** *(verified live count this session: still exactly 719, unchanged)* | Medium | `blogs.html` client-side array | Generic `title: "Redirecting..."` / `excerpt: "Tax services Pakistan 2026."` entries are either dead links to slugs that were never built, or duplicate/shadow real pages that were built under the same slug later — one of the latter already caused a real duplicate-id parse error earlier in this project (`tax-consultant-kamoke`). Every future page addition risks hitting another collision until this is cleared. | Cross-reference each of the 719 stub `id`/`link` values against real files in `blogs/`: delete the stub if no matching file exists and none is planned; if a matching file exists, replace the stub's placeholder fields with the real title/excerpt. Scriptable — a one-time Node pass, not manual per-entry work. | ~2-4 hours for a script pass + spot-check |
| **609 real, published blog files have zero entry in the `blogPosts` JS array** *(verified live: 1,403 files in `blogs/`, only 1,279 array entries, 609 of the files aren't among them)* | Low-Medium | `blogs.html`'s interactive/filterable blog listing UI | **Not a crawlability or indexation problem** — verified live that all 609 of these files are still present in the static, SEO-crawlable `<a href>` link list on the same page (0 files are missing from *both* the JS array and the static list), so search engines and AI crawlers can still find and index them. The real cost is user-facing: someone browsing/filtering blogs.html interactively won't see these 609 posts in the dynamic list, only in the plain static index further down the page. | Lower priority than the stub cleanup above, and can likely be resolved by the same script pass (once stubs are cleaned, backfill real array entries for the 609 files that don't have one) rather than as a separate project. | Folds into the stub-cleanup effort above |
| **No connected analytics/measurement** *(verified: no GA4/GTM snippet or Search Console verification file found in this project's files this session)* | High (for the business goal, not a technical defect) | Whole site | Every claim about what's converting or ranking in this project is currently a structural inference, not measured fact — this blocks the entire "which commercial content actually works" question the operator is trying to answer. | See strategy-director's roadmap, Phase 1 — this is an `analytics-engineer` task, not tech-auditor's to implement. | Flagged, not tech-auditor's mandate to fix |
| **`robots.txt` — no issue found, confirmed correct** *(verified live: read the actual file)* | None | — | Fully open to Googlebot/Bingbot/DuckDuckBot/YandexBot and every major AI crawler (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, Applebot-Extended, meta-externalagent, etc.), with only genuinely aggressive scrapers (MJ12bot, DotBot disallowed; AhrefsBot/SemrushBot throttled) restricted. Sitemap directive present and correct. This is already exactly right for a project prioritizing both classic SEO and GEO/AI visibility — no action needed. | — | — |
| **No `package.json`/CMS/framework** *(verified: confirmed absent)* | None (informational) | — | Confirms the stack is plain static HTML — 1,403 blog files + ~164 other pages (1,567 total) is well under any real crawl-budget threshold (~10k+ per KNOWLEDGE.md), so crawl budget is not a real risk at this scale despite the large page count. No framework migration is warranted. | — | — |
| **Broken internal link baseline: 926, overwhelmingly one known pattern** *(verified: this project's own `site-health-check.js`, run repeatedly through this session's batches)* | Low | Sitewide | The count has grown proportionally with every new page added this session (exactly +1 per new file, every single time, tracked explicitly across 6 separate batches) — confirmed this is the same pre-existing `/authors/aitsaam-ali` byline-link pattern already flagged as a known, accepted issue, not new breakage. | If the operator wants this actually fixed rather than just tracked as stable: create the missing `/authors/aitsaam-ali` page (or fix the byline link pattern sitewide) once, which would resolve the bulk of this count in one pass rather than letting it keep growing by 1 per future page. | Low — single-page fix, sitewide find-replace or one new file |
| **Sitemap/blogPosts/static-list count reconciliation** *(verified live)* | Informational | `blogs.html`, `sitemap.xml` | Sitemap: 1,560 URLs. `blogPosts` array: 1,279 (719 stale). Static link list: 1,269. Real blog files: 1,403. These four numbers don't need to match exactly (sitemap includes non-blog pages too), but the spread confirms the stub-cleanup fix above is the single highest-leverage technical task available right now — it's the only issue on this list that's both real, quantified, and already fully diagnosed. | — | — |

## What's already handled — not manufacturing a problem

Per SKILL.md's explicit instruction not to invent issues to look thorough: robots.txt, sitemap presence, AI-crawler access, and the static-HTML architecture are all already correct for this project's scale and goals. The only genuinely open, quantified technical defect is the stale-stub cleanup — everything else on this list is either already fine, already tracked as stable/accepted, or outside this role's mandate (analytics belongs to analytics-engineer).

---
**Verified this session:** all file counts, array parsing, robots.txt content, stub count, orphan-file cross-reference.
**Not checked this session (out of scope for a file-system audit):** live Search Console coverage report, real Core Web Vitals field data (no CrUX/PageSpeed check run) — flagged for `cwv-engineer` if a full tech-check workflow is run later.
